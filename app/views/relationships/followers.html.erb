<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-12">
      <h2> Followed Users</h2>
      <% if @users.exists? %>
        <%= render "users/user_index", users: @users %>
      <% else %>
        <p>ユーザーはいません</p>
      <% end %>
    </div>
  </div>
</div>